const { color } = require('discord-yui')

module.exports = {
    name: 'color',
    description: "Get an random color.",
    category: "🔰 Util",
    usage: `${process.env.prefix}color`,
    cooldown: 2,
    nsfw: false,

    async execute(message, args, client) {

        //color(message, message.channel)
    }
};